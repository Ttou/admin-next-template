import { type App } from 'vue'

/**
 * - vxe-table-plugin-element 中用到的组件
 * - 其他页面中用到的组件
 * - Tsx 中引用的组件可以不用在这里注册
 */
export function useElementPlus(app: App) {
  // vxe-table-plugin-element 中用到的组件
}
